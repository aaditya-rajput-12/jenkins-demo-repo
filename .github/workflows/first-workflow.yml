name : First Github Actions

on:
    push:
        branches:
          - main
jobs:
  demo-job:
    runs-on: ubuntu-latest

    
    steps:
      - name: Print Hello
        run : echo "Hello from Github Actions"
